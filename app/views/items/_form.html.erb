<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= # noinspection RubyQuotedStringsInspection
              pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.collection_select :order_id, Order.order(:custumer_name),:id,:custumer_name, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.collection_select :product_id, Product.order(:name),:id,:name, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity, :in => 0..1000, :step => 1 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
