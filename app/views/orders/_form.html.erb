<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h4 class="alert alert-danger"><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h4>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
            <div class="alert alert-warning"><%= message %></div>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :custumer_name %><br>
    <%= f.text_field :custumer_name, class: "form-control" %>
  </div>
    <br>
  <div class="actions">
    <%= f.submit class: "btn btn-primary"%>
  </div>
<% end %>
